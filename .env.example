# ===========================================
# StayHub Environment Configuration Example
# ===========================================
# Copy this file to .env and fill in your values
# DO NOT commit .env file to version control!

# =============================================================================
# PostgreSQL Config (Local Docker - for local development without Supabase)
# =============================================================================
POSTGRES_DB=stayhub_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@stayhub.com
PGADMIN_DEFAULT_PASSWORD=admin123

# =============================================================================
# Supabase Database Configuration
# =============================================================================
# Get these values from Supabase Dashboard:
# Project Settings -> Database -> Connection Pooling -> Session Mode

DB_HOST=aws-1-ap-southeast-1.pooler.supabase.com
DB_PORT=5432
DB_NAME=postgres
DB_USERNAME=postgres.your-project-id
DB_PASSWORD=your-database-password

# JDBC URL for Spring Boot (Supabase Pooler - Session Mode)
DB_URL=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres

# =============================================================================
# Mail Configuration (Gmail SMTP)
# =============================================================================
# For Gmail: Enable 2FA and create App Password
# https://myaccount.google.com/apppasswords

MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# =============================================================================
# JWT Configuration
# =============================================================================
# Generate a secure random key (min 256 bits / 64 hex chars)
# Use: openssl rand -hex 32

JWT_SECRET_KEY=your-256-bit-secret-key-in-hex-format

# =============================================================================
# Frontend Configuration
# =============================================================================
# For local development:
FRONTEND_URL=http://localhost:3000
VITE_API_BASE_URL=http://localhost:8080/api

# For production (set these on Render/Vercel):
# FRONTEND_URL=https://www.stay-hub.site
# VITE_API_BASE_URL=https://api.stay-hub.site/api

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins
# For local: http://localhost:3000,http://localhost:5173
# For production: https://www.stay-hub.site,https://stay-hub.site
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# =============================================================================
# OAuth2 Configuration
# =============================================================================
# Google Console: https://console.cloud.google.com/apis/credentials
# Redirect URI: http://localhost:8080/login/oauth2/code/google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Grafana user & password
GRAFANA_ADMIN_USER=your-grafana-id
GRAFANA_ADMIN_PASSWORD=your-grafana-password

# 3. Application:
#    - Set SPRING_PROFILE=prod for production
#    - Set SPRING_JPA_DDL_AUTO=validate or none in production
#    - Configure proper logging levels
#
# 4. Docker:
#    - Use specific version tags for images
#    - Set resource limits for containers
#    - Configure proper restart policies
#
# =============================================================================