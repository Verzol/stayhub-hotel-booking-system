# Test Configuration
spring.application.name=stayhub-test

# H2 Database for Testing
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Mail Configuration (Dummy)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# Security Configuration (Test Key - Random Dummy Value)
application.security.jwt.secret-key=746573742d7365637265742d6b65792d666f722d756e69742d74657374732d6f6e6c792d646f2d6e6f742d7573652d696e2d70726f64

# OAuth2 Configuration (Dummy for Tests)
spring.security.oauth2.client.registration.google.client-id=fake-client-id
spring.security.oauth2.client.registration.google.client-secret=fake-client-secret
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.registration.google.authorization-grant-type=authorization_code

# Frontend URL for tests
app.frontend.url=http://localhost:3000

# CORS Configuration for tests
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173